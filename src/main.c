#include <stdio.h>
#include <stdlib.h>
int main()
{
    int m, n, r;
    printf("input parametrs(m,n,r)(cherez probel)");
    scanf("%d %d %d", &m, &n, &r);
    printf("circle( %d , %d , %d )", m, n, r);
    return 0;
}
